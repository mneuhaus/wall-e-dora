nodes:
  - id: listener_1
    path: nodes/listener-1/listener_1/main.py
    inputs:
      speech-2: web/my_output_id

  # - id: gamepad
  #   path: nodes/gamepad/gamepad/main.py
  #   inputs:
  #     tick: dora/timer/millis/100
  #   outputs:
  #     - gamepad_input

  - id: power
    path: nodes/power/power/main.py
    inputs:
      tick: dora/timer/secs/10
    outputs:
      - voltage
      - current
      - power
      - soc
      - runtime
      - shutdown

  - id: web
    path: nodes/web/web/main.py
    inputs:
      tick: dora/timer/millis/100
      voltage: power/voltage
      current: power/current
      power: power/power
      soc: power/soc
      runtime: power/runtime
      shutdown: power/shutdown
    outputs:
      - my_output_id
      - slider_input

  # - id: tracks
  #   path: nodes/tracks/tracks/main.py
  #   inputs:
  #     tick: dora/timer/millis/100
  #   outputs:
  #     - my_output_id
