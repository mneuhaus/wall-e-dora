/**
 * Common Components
 * 
 * Entry point for all common components that can be reused across the application.
 * This provides a centralized import for all shared UI components.
 */

import * as inputs from './inputs';

export { inputs };

export default {
  inputs
};