/**
 * Control Component Styles
 * 
 * Provides consistent styling for control components in the header.
 * Makes control icons larger and more touch-friendly.
 * Extends the statusIconStyles for control components.
 */
import { statusIconStyles } from './StatusIconStyles';

// Export the status icon styles directly for controls
export const controlStyles = statusIconStyles;